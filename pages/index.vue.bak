<template>
  <div class="bg-gradient-to-b from-amber-50 to-white py-12 md:py-20">
    <!-- Hero Section -->
    <div class="mb-12 md:mb-16 text-center">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 mb-6">
        Discover Sacred <span class="text-amber-600">Jain Tirths</span>
      </h1>
      <p class="text-lg md:text-xl text-gray-600 max-w-2xl mx-auto">
        Explore spiritual pilgrimage sites with detailed information, nearby facilities, and interactive maps
      </p>
    </div>

    <!-- Search and Filter Section -->
    <div class="mb-12">
      <!-- Search Bar -->
      <div class="mb-6 max-w-2xl mx-auto">
        <div class="relative">
          <Icon name="Search" :size="20" class="absolute left-4 top-3 text-gray-400" />
          <input
            v-model="searchTerm"
            type="text"
            placeholder="Search temples by name, city, or state..."
            class="w-full pl-12 pr-4 py-3 rounded-lg border border-gray-300 focus:border-amber-600 focus:ring-2 focus:ring-amber-200 outline-none transition-all"
          />
        </div>
      </div>

      <!-- Filters -->
      <div class="grid grid-cols-1 md:grid-cols-4 gap-4 max-w-4xl mx-auto">
        <!-- State Filter -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">State</label>
          <select
            v-model="filters.state"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:border-amber-600 focus:ring-2 focus:ring-amber-200 outline-none transition-all"
          >
            <option value="">All States</option>
            <option value="Gujarat">Gujarat</option>
            <option value="Rajasthan">Rajasthan</option>
            <option value="Karnataka">Karnataka</option>
            <option value="Maharashtra">Maharashtra</option>
            <option value="Madhya Pradesh">Madhya Pradesh</option>
          </select>
        </div>

        <!-- Sect Filter -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">Sect</label>
          <select
            v-model="filters.sect"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:border-amber-600 focus:ring-2 focus:ring-amber-200 outline-none transition-all"
          >
            <option value="">All Sects</option>
            <option value="Shwetambar">Shwetambar</option>
            <option value="Digambar">Digambar</option>
          </select>
        </div>

        <!-- Type Filter -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">Type</label>
          <select
            v-model="filters.type"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:border-amber-600 focus:ring-2 focus:ring-amber-200 outline-none transition-all"
          >
            <option value="">All Types</option>
            <option value="Gyan-sthan">Gyan-sthan</option>
            <option value="Siddhakshetra">Siddhakshetra</option>
            <option value="Atishay-Kshetra">Atishay Kshetra</option>
            <option value="Other">Other</option>
          </select>
        </div>

        <!-- Clear Filters -->
        <div class="flex items-end">
          <button
            @click="clearFilters"
            class="w-full px-4 py-2 bg-gray-200 text-gray-800 font-semibold rounded-lg hover:bg-gray-300 transition-colors"
          >
            Clear Filters
          </button>
        </div>
      </div>
    </div>

    <!-- Results Info -->
    <div class="mb-6 text-center text-gray-600">
      <p class="text-sm md:text-base">
        Showing <span class="font-semibold text-gray-900">{{ filteredCount }}</span>
        {{ filteredCount === 1 ? 'temple' : 'temples' }}
      </p>
    </div>

    <!-- Loading State -->
    <div v-if="loading" class="flex justify-center items-center py-12">
      <div class="animate-spin">
        <Icon name="Loader2" :size="32" class="text-amber-600" />
      </div>
    </div>

    <!-- Error State -->
    <div v-if="error" class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg text-red-700">
      {{ error }}
    </div>

    <!-- Tirth Cards Grid -->
    <div v-if="!loading && filteredTirths.length > 0" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <TirthCard v-for="tirth in filteredTirths" :key="tirth.id" :tirth="tirth" />
    </div>

    <!-- Empty State -->
    <div v-if="!loading && filteredTirths.length === 0" class="text-center py-12">
      <Icon name="MapPin" :size="48" class="text-gray-300 mx-auto mb-4" />
      <h3 class="text-xl font-semibold text-gray-600 mb-2">No Temples Found</h3>
      <p class="text-gray-500">Try adjusting your search or filter criteria</p>
    </div>
  </div>
</template>
